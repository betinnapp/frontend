/*
 * SubmodulesList Messages
 *
 * This contains all the text for the SubmodulesList container.
 */

import { defineMessages } from 'react-intl'

export const scope = 'app.containers.SubmodulesList'

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the SubmodulesList container!',
  },
})
