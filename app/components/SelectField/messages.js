/*
 * SelectField Messages
 *
 * This contains all the text for the SelectField component.
 */

import { defineMessages } from 'react-intl'

export const scope = 'app.components.SelectField'

export default defineMessages({
  selectAnOption: {
    id: `${scope}.selectAnOption`,
    defaultMessage: 'Selecione uma opção...',
  },
})
